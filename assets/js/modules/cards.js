/**
 * cards.js — Project card hover (image → video crossfade)
 *
 * On hover: GSAP fade static image out, autoplay <video> in + scale(1.03).
 * TODO: implement when index.html project grid is built.
 */

export function initCards() {
  // Placeholder — implementation pending
}
