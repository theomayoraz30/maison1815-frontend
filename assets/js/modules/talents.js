/**
 * talents.js — Talent name hover (floating asset positioning)
 *
 * On hover: display image/video asset at random viewport position.
 * GSAP: opacity 0→1, scale 0.9→1, duration 0.25s.
 * TODO: implement when talents.html is built.
 */

export function initTalents() {
  // Placeholder — implementation pending
}
